<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.project_99">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_99">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Puzzlegame" android:label="미니게임 즐기기" />
        <activity android:name=".Buy" android:label="메뉴 목록" />
        <activity android:name=".Basket" android:label="장바구니 목록" />
        <activity android:name=".BuyList" android:label="주문내역" />
        <activity android:name=".HotandIce" android:label="." />

    </application>

</manifest>